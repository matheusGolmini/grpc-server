syntax = "proto3";

package example.shared;

// Informações de auditoria comuns a múltiplos contextos
message AuditInfo {
  string createdAt = 1; // ISO 8601 datetime
  string updatedAt = 2; // ISO 8601 datetime
}